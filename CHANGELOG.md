# Changelog

## Unreleased
FEATURES
* [#23](https://github.com/mocachain/moca-cmd/pull/23) Add `--delegate` to `object put` and `object update`: the primary SP creates the object, or replaces its content, on chain on the user's behalf.

## v0.2.0
FEATURES
